# Harbor — managed by ArgoCD Helm Application (see application.yaml)
# Admin password: Vault secret/harbor (out-of-band k8s Secret harbor-admin-secret)
# Core secret key: Vault secret/harbor/core (out-of-band k8s Secret harbor-core-secret)
