apiVersion: apps/v1
kind: Deployment
metadata:
  name: chill-farm
  namespace: chill-farm
  labels:
    app: chill-farm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chill-farm
  template:
    metadata:
      labels:
        app: chill-farm
      annotations:
        # bump this to force a rollout when the site content changes
        chill-farm/content-rev: "26"
    spec:
      containers:
      - name: nginx
        image: nginx:1.27-alpine
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        volumeMounts:
        - name: site
          mountPath: /usr/share/nginx/html
          readOnly: true
        - name: nginx-charset
          mountPath: /etc/nginx/conf.d/charset.conf
          subPath: charset.conf
          readOnly: true
        resources:
          requests:
            memory: "32Mi"
            cpu: "20m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 3
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 20
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: false
      volumes:
      - name: site
        configMap:
          name: chill-farm-site
          items:
          - key: index.html
            path: index.html
          - key: careers.html
            path: careers/index.html
          - key: pasture.html
            path: pasture/index.html
          - key: ranch.html
            path: ranch/index.html
          - key: pond.html
            path: pond/index.html
          - key: cross.html
            path: cross/index.html
          - key: splash.html
            path: splash/index.html
          - key: hall.html
            path: hall/index.html
          - key: news.html
            path: news/index.html
          - key: og-image.png
            path: og-image.png
          - key: manifest.json
            path: manifest.json
          - key: sw.js
            path: sw.js
          - key: icon-192.png
            path: icon-192.png
          - key: icon-512.png
            path: icon-512.png
      - name: nginx-charset
        configMap:
          name: chill-farm-nginx
          items:
          - key: charset.conf
            path: charset.conf
