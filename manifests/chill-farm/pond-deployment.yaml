apiVersion: apps/v1
kind: Deployment
metadata:
  name: chill-pond
  namespace: chill-farm
  labels:
    app: chill-pond
spec:
  # single replica: authoritative in-memory pond state (one shared pond)
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: chill-pond
  template:
    metadata:
      labels:
        app: chill-pond
      annotations:
        chill-pond/src-rev: "1"
    spec:
      containers:
      - name: pond
        image: node:20-alpine
        imagePullPolicy: IfNotPresent
        command: ["node", "/app/server.js"]
        env:
        - name: PORT
          value: "8080"
        - name: OLLAMA_URL          # DGX Spark Ollama for the occasional whimsical AI catch
          value: "http://10.0.129.94:11434"
        - name: OLLAMA_MODEL
          value: "llama3.1:8b"
        ports:
        - name: ws
          containerPort: 8080
          protocol: TCP
        volumeMounts:
        - name: src
          mountPath: /app/server.js
          subPath: server.js
          readOnly: true
        resources:
          requests:
            memory: "48Mi"
            cpu: "50m"
          limits:
            memory: "192Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /healthz
            port: ws
          initialDelaySeconds: 2
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /healthz
            port: ws
          initialDelaySeconds: 5
          periodSeconds: 20
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
      volumes:
      - name: src
        configMap:
          name: chill-pond-src
          items:
          - key: server.js
            path: server.js
