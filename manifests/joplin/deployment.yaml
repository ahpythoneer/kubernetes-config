apiVersion: apps/v1
kind: Deployment
metadata:
  name: joplin-server
  namespace: joplin
  labels:
    app: joplin-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: joplin-server
  template:
    metadata:
      labels:
        app: joplin-server
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "joplin"
    spec:
      serviceAccountName: joplin
      containers:
      - name: joplin-server
        image: joplin/server:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 22300
          protocol: TCP
        env:
        - name: APP_BASE_URL
          value: "https://joplin.racktocloud.com"
        - name: DB_CLIENT
          value: "pg"
        - name: TRUSTED_DOMAINS
          value: "localhost,127.0.0.1,joplin-server:22300,10.100.0.0/16"
        - name: POSTGRES_HOST
          valueFrom:
            secretKeyRef:
              name: joplin-db
              key: postgres_host
        - name: POSTGRES_PORT
          valueFrom:
            secretKeyRef:
              name: joplin-db
              key: postgres_port
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: joplin-db
              key: postgres_user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: joplin-db
              key: postgres_password
        - name: POSTGRES_DATABASE
          valueFrom:
            secretKeyRef:
              name: joplin-db
              key: postgres_database
        livenessProbe:
          httpGet:
            path: /api/ping
            port: 22300
          initialDelaySeconds: 90
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 10
        readinessProbe:
          httpGet:
            path: /api/ping
            port: 22300
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 10
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"